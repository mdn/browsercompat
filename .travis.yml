# Config file for automatic testing at travis-ci.org

language: python

python:
  - "3.4"
  - "2.7"
  - "2.6"

install: pip install -r requirements.txt coveralls
script: python manage.py test --with-coverage --cover-package=webplatformcompat
after_success: coveralls
